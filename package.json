{
  "name": "ufaas",
  "version": "0.0.1",
  "description": "μFaaS - Frontend as a Service.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently 'cd src/server && yarn dev' 'cd src/frontend && yarn dev'",
    "build": "concurrently 'cd src/server && yarn build' 'cd src/frontend && yarn build'"
  },
  "keywords": [
    "FaaS"
  ],
  "author": "liril <god@liril.net>",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^3.4.0",
    "lerna": "^2.0.0-rc.5",
    "tslint": "^5.4.3",
    "tslint-config-prettier": "^1.1.0",
    "tslint-react": "^3.0.0"
  }
}
